% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CombiningClassifier.R
\name{my_logistic}
\alias{my_logistic}
\title{Logistic Regression Implementation}
\usage{
my_logistic(X, y, max_iter = 100, tol = 1e-06)
}
\arguments{
\item{X}{A matrix or data frame of predictors (independent variables).}

\item{y}{A binary vector (0/1) of outcomes (dependent variable).}

\item{max_iter}{An integer specifying the maximum number of iterations. Default is 100.}

\item{tol}{A numeric value specifying the convergence tolerance. Default is 1e-6.}
}
\value{
A list containing:
\describe{
\item{coefficients}{A vector of estimated coefficients, including the intercept.}
\item{fitted.values}{A vector of predicted probabilities for each observation.}
\item{iterations}{The number of iterations taken to converge.}
\item{converged}{Logical, indicating whether the algorithm converged.}
}
}
\description{
Implements logistic regression using iterative optimization (Newton-Raphson method).
}
\examples{
# Example data
set.seed(123)
X <- data.frame(x1 = rnorm(100), x2 = rnorm(100))
y <- sample(c(0, 1), 100, replace = TRUE)

# Fit logistic regression
result <- my_logistic(X, y)
print(result$coefficients)
print(result$fitted.values)
}
